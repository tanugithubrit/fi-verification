package ai.lentra.repository.lookups;

import ai.lentra.modal.lookup.SpouseCitizenship;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpouseCitizenshipRepository extends JpaRepository<SpouseCitizenship, Integer> {
}
